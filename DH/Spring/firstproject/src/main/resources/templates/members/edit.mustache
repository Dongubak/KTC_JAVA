{{>layouts/header}}
{{#memvberArticle}}
<form class="container" action = "/members/update" method="post">
    <input name = "id" type="hidden"  value="{{id}}">
    <div>
        <label class="form-label">이메일</label>
        <input type="email" class = "form-control" name="email" value="{{memvberArticle.email}}">
    </div>
    <div class = "mb-3">
        <label class="form-label">비밀번호</label>
        <input type="text" class="form-control" name="password">{{memvberArticle.password}}
    </div>
    <button type="submit" class="btn btn-primary">수정하기</button>
    <a href="/members/{{memvberArticle.id}}">Back</a>
</form>
{{/memvberArticle}}
{{>layouts/footer}}